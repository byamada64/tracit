server {
    listen 3000;
    server_name _;

    root /usr/share/nginx/html;

    # Serve static files
    location /_next/ {
        try_files $uri $uri/ =404;
    }

    # Serve everything else (fallback to index.html)
    location / {
        try_files $uri /index.html;
    }
}
