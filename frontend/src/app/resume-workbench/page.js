"use client";

import PageContainer from "@/components/layout/PageContainer";
import ToolsRow from "@/components/tools/ToolsRow";

export default function ResumeWorkbenchPage() {
  return (
    <PageContainer bannerKey="resumeWorkbench">
      {/* Icons directly under banner */}
      <ToolsRow />

      {/* Removed duplicate title + paragraph */}
    </PageContainer>
  );
}
